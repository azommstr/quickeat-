const carItems = JSON.parse(localStorage.getItem("cart-item"));
const carItems_El = document.querySelector(".cart_item");

cart_item.forEach((item) => {
    const divEL = document.createElement("div");
    divEL.classList.add(cart_item);

});